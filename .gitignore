# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
*.xcresult
Payload/
*.ipa

# SwiftPM
.build/
.swiftpm/
# Package.resolved is committed on purpose: CI must resolve the same
# ZIPFoundation revision as local development, not whatever is newest.

# XcodeGen-generated
App/Resources/Info.plist

# Misc
.DS_Store
*.swp

# SwiftPM scratch directories used during development
.build*/
